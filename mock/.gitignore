# Generated by build.mjs — exactly the text each document record will hold.
# Regenerate with `node mock/build.mjs`; it is a duplicate of documents/ and
# there is no reason to carry a second copy in the repository.
parts/

# Generated by emit-sql.mjs from blob.json. The two hand-written files are kept.
sql/*.sql
!sql/00-users.sql
!sql/99-teardown.sql
